//var let const
//var is alive for entire program that is global
//let is alive in a scope that is local
//const that means the value can't be modified
//let and var can be modified
//const we can change/manipulate the const itself
//eg const*3
const pi=3.14;
let radius=4;
let area=pi*radius*radius;
console.log(area);
let parameter =2*pi*radius;
console.log(parameter);
